export class Category {
    id: number;
    label: string;
    image: string;

    constructor(
        id: number,
        label: string,
        image: string

    ) {
        this.id = id;
        this.label = label;
        this.image = image;
    }
}
